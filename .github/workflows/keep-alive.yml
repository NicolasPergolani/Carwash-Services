name: Keep API Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes
  workflow_dispatch:  # Allow manual trigger

jobs:
  ping-api:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -f https://carwash-services.onrender.com/ || echo "Ping failed"