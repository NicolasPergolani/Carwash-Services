# Server Configuration
PORT=3003
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# MongoDB Atlas Configuration
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/carwash_services?retryWrites=true&w=majority

# JWT Secret (if needed for token validation)
JWT_SECRET=your_jwt_secret_here